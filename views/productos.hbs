<main class="container pt-5">
    <h2 class="mb-5">Listado productos</h2>
    {{#if productos.length}}
        {{> tablaProductos}}
    {{else}}
    <div class="alert alert-warning" role="alert">
        No hay productos para mostrar
    </div>
    {{/if}}
</main>